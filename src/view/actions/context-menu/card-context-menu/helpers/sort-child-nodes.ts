import { MandalaView } from 'src/view/view';

export const sortChildNodes = (
    view: MandalaView,
    activeNode: string,
    order: 'ascending' | 'descending',
) => {
    void view;
    void activeNode;
    void order;
};
